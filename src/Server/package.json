{
  "name": "server",
  "version": "1.0.0",
  "description": "json data",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "author": "arjun",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
